Setup ομάδας για UniBite
1. Κατεβάζουμε τα προγράμματα
VS Code

https://code.visualstudio.com/

Node.js LTS

https://nodejs.org/

Μετά την εγκατάσταση, έλεγχος σε terminal:

node -v
npm -v
XAMPP

https://www.apachefriends.org/

Μετά την εγκατάσταση:

XAMPP Control Panel → Start MySQL

Αν η MySQL δεν ξεκινάει λόγω port 3306, αλλάζουμε port σε 3307.

2. Βάζουμε VS Code extensions

Στο VS Code:

Ctrl + Shift + X

Εγκαθιστούμε:

Live Server
Prettier - Code formatter
Thunder Client

Προαιρετικά:

MySQL
ES7+ JavaScript snippets
3. Ρυθμίζουμε Git

Σε terminal:

git config --global user.name "Το Όνομά σου"
git config --global user.email "το-email-σου@example.com"

Έλεγχος:

git config --list
4. Κατεβάζουμε το project από GitHub

Πηγαίνουμε στον φάκελο που θέλουμε να μπει το project, π.χ. Desktop:

cd Desktop

Μετά:

git clone https://github.com/USERNAME/unibite.git

Βάζουμε το σωστό link του repo.

5. Μπαίνουμε στον φάκελο του project
cd unibite
6. Κατεβάζουμε τα dependencies
npm install

Αυτό κατεβάζει ό,τι χρειάζεται το backend, π.χ.:

express
cors
mysql2
7. Κάθε φορά που δουλεύουμε

Πρώτα παίρνουμε τις τελευταίες αλλαγές:

git pull

Μετά κάνουμε αλλαγές στον κώδικα.

8. Για να τρέξει το project

Ανοίγουμε XAMPP:

Start MySQL

Μετά στο VS Code terminal:

node server.js

Αν όλα είναι σωστά, ανοίγουμε:

http://localhost:3000
9. Όταν τελειώσουμε αλλαγές
git add .
git commit -m "περιγραφή αλλαγών"
git push


ΠΩΣ ΧΩΡΙΖΕΤΕ ΤΟ PROJECT ΠΑΝΩ ΣΤΑ FILES

⚙️ 👤 ΑΤΟΜΟ 1 — CORE

👉 Δουλεύει κυρίως εδώ:

server.js
db.js
database/unibite.sql
auth.js
login.html
index.html (structure μόνο)
style.css (basic theme)
🔥 ΤΙ ΚΑΝΕΙ
✔ login / register (auth.js)
✔ localStorage user
✔ roles (cook / consumer)
✔ database setup (SQL)
✔ basic API routes
✔ layout (header + sections)


👨‍🍳 👤 ΑΤΟΜΟ 2 — COOK

👉 Δουλεύει εδώ:

server.js
script.js
index.html (requests section)
🔥 ΤΙ ΚΑΝΕΙ
✔ accept request
✔ reject request
✔ loadRequests() για cook
✔ κουμπιά:
   - Αποδοχή
   - Απόρριψη
✔ μείωση μερίδων


🍽️ 👤 ΑΤΟΜΟ 3 — CONSUMER

👉 Δουλεύει εδώ:

script.js
index.html (modal + meals)
🔥 ΤΙ ΚΑΝΕΙ
✔ openRequest()
✔ submitRequest()
✔ loadMyRequests()
✔ completeRequest()
✔ modal UI
✔ κουμπί "Ζήτηση"
✔ κουμπί "Ολοκλήρωση"

chat: Web εφαρμογη Food Sharing



ALTER TABLE requests ADD portions INT;